package main

const usage = `

Usage:
  oclmagic --hashcat <hashcatexec>
  oclmagic -h | --help
  oclmagic --version

Options:
  -h, --help                  Show usage.
  --version                   Show version.
  --hashcat <hashcatexec>    HashCat executable
`
